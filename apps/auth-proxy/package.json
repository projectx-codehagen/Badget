{
  "name": "@projectx/auth-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "clean": "rm -rf .turbo node_modules",
    "dev": "nitro dev --port 3001",
    "lint": "eslint .",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@auth/core": "^0.23.0"
  },
  "devDependencies": {
    "@projectx/eslint-config": "^0.2.0",
    "@projectx/prettier-config": "^0.1.0",
    "@projectx/tailwind-config": "^0.1.0",
    "@projectx/tsconfig": "^0.1.0",
    "eslint": "^8.56.0",
    "nitropack": "^2.8.1",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  },
  "eslintConfig": {
    "root": true,
    "extends": [
      "@projectx/eslint-config/base"
    ]
  },
  "prettier": "@projectx/prettier-config"
}